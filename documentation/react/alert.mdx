---
title: Tailwind CSS Alert for React - Material Tailwind
description: Customise your web projects with our easy-to-use alert component for Tailwind CSS and React using Material Design guidelines.
navigation:
  [
    "alert",
    "alert-variants",
    "alert-colors",
    "alert-with-icon",
    "alert-with-list",
    "alert-dismissible",
    "custom-alert-animation",
    "custom-alert-close-icon",
    "alert-with-content",
    "props",
    "theme",
  ]
related:
  [
    {
      title: "Props",
      route: "props/alert",
      description: "Learn more about props definition and types of alert component.",
    },
    {
      title: "Theme",
      route: "theme/alert",
      description: "Learn how to customize the theme and styles for alert component.",
    },
  ]
github: alert
prev: accordion
next: avatar
---

# Tailwind CSS Alert - React

Get started on your web projects with our Tailwind CSS <Code>Alert</Code> which provides contextual feedback messages for typical user actions.

An <Code>Alert</Code> displays a short and important message attracting the user's attention without interrupting the user's task. Use this element when you need to show highly-important messages to users or when you need a persistent static container that is closable by user actions.

See below our simple <Code>Alert</Code> example that you can use in your Tailwind CSS and React project. The example also comes in different styles and colors, so you can adapt it easily to your needs.

<br />

<CodePreview id="alert" link="alert#alert" component={<Alert>A simple alert for showing message.</Alert>}>
```jsx
import { Alert } from "@material-tailwind/react";

export default function Example() {
  return <Alert>A simple alert for showing message.</Alert>;
}
```
</CodePreview>

---

## Alert Variants

The <Code>Alert</Code> component comes with 3 different variants that you can change it using the <Code>variant</Code> prop.

<CodePreview id="alert-variants" link="alert#alert-variants" component={
  <div className="flex w-full flex-col gap-2">
    <Alert>A simple filled alert for showing message.</Alert>
    <Alert color="red" variant="gradient">
      <span>A simple gradient alert for showing message.</span>
    </Alert>
    <Alert color="green" variant="outlined">
      <span>A simple outlined alert for showing message.</span>
    </Alert>
    <Alert color="orange" variant="ghost">
      <span>A simple ghost alert for showing message.</span>
    </Alert>
  </div>
}>
```jsx
import { Alert } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col gap-2">
      <Alert>A simple filled alert for showing message.</Alert>
      <Alert color="red" variant="gradient">
        <span>A simple gradient alert for showing message.</span>
      </Alert>
      <Alert color="green" variant="outlined">
        <span>A simple outlined alert for showing message.</span>
      </Alert>
      <Alert color="orange" variant="ghost">
        <span>A simple ghost alert for showing message.</span>
      </Alert>
    </div>
  );
}
```
</CodePreview>

---

## Alert Colors

The <Code>Alert</Code> component comes with 19 different colors that you can change it using the <Code>color</Code> prop.

<CodePreview id="alert-colors" link="alert#alert-colors" component={
  <div className="flex w-full flex-col gap-2">
    <Alert>An info alert for showing message.</Alert>
    <Alert color="red">An error alert for showing message.</Alert>
    <Alert color="green">A success alert for showing message.</Alert>
    <Alert color="amber">A warning alert for showing message.</Alert>
  </div>
}>
```jsx
import { Alert } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex w-full flex-col gap-2">
      <Alert color="blue">An info alert for showing message.</Alert>
      <Alert color="red">An error alert for showing message.</Alert>
      <Alert color="green">A success alert for showing message.</Alert>
      <Alert color="amber">A warning alert for showing message.</Alert>
    </div>
  );
}
```
</CodePreview>

---

## Alert with Icon

You can add an icon at the beginning of <Code>Alert</Code> component using the <Code>icon</Code> prop.

<CodePreview id="alert-with-icon" link="alert#alert-with-icon" component={
  <div className="flex w-full flex-col gap-2">
    <Alert
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      {`A simple alert with icon for showing message`}
    </Alert>
    <Alert
      color="red"
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      {`A simple alert with icon for showing message`}
    </Alert>
    <Alert
      color="green"
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      {`A simple alert with icon for showing message`}
    </Alert>
    <Alert
      color="amber"
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      {`A simple alert with icon for showing message`}
    </Alert>
  </div>
}>
```jsx
import { Alert } from "@material-tailwind/react";
import { InformationCircleIcon } from "@heroicons/react/24/outline"

export default function Example() {
  return (
    <div className="flex w-full flex-col gap-2">
      <Alert
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        A simple alert with icon for showing message
      </Alert>
      <Alert
        color="red"
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        A simple alert with icon for showing message
      </Alert>
      <Alert
        color="green"
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        A simple alert with icon for showing message
      </Alert>
      <Alert
        color="amber"
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        A simple alert with icon for showing message
      </Alert>
    </div>
  );
}
```
</CodePreview>

---

## Alert with List

You can add any element you want for the <Code>Alert</Code> component, below is a example of alert with a list inside.

<CodePreview id="alert-with-list" link="alert#alert-with-list" component={
  <div className="flex w-full flex-col gap-2">
    <Alert
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      <Typography className="font-medium">Ensure that these requirements are met:</Typography>
      <ul class="mt-2 ml-2 list-disc list-inside">
        <li>At least 10 characters (and up to 100 characters)</li>
        <li>At least one lowercase character</li>
        <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
      </ul>
    </Alert>
    <Alert
      variant="gradient"
      color="red"
      icon={
        <OutlineIcons.InformationCircleIcon
          strokeWidth={2}
          className="h-6 w-6"
        />
      }
    >
      <Typography className="font-medium">Ensure that these requirements are met:</Typography>
      <ul class="mt-2 ml-2 list-disc list-inside">
        <li>At least 10 characters (and up to 100 characters)</li>
        <li>At least one lowercase character</li>
        <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
      </ul>
    </Alert>
    <Alert
      variant="outlined"
      color="green"
      icon={
        <SolidIcons.InformationCircleIcon className="h-6 w-6" />
      }
    >
      <Typography className="font-medium">Ensure that these requirements are met:</Typography>
      <ul class="mt-2 ml-2 list-disc list-inside">
        <li>At least 10 characters (and up to 100 characters)</li>
        <li>At least one lowercase character</li>
        <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
      </ul>
    </Alert>
    <Alert
      variant="ghost"
      color="orange"
      icon={
        <SolidIcons.InformationCircleIcon className="h-6 w-6" />
      }
    >
      <Typography className="font-medium">Ensure that these requirements are met:</Typography>
      <ul class="mt-2 ml-2 list-disc list-inside">
        <li>At least 10 characters (and up to 100 characters)</li>
        <li>At least one lowercase character</li>
        <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
      </ul>
    </Alert>
  </div>
}>
```jsx
import { Alert } from "@material-tailwind/react";
import { InformationCircleIcon } from "@heroicons/react/24/outline"
import { InformationCircleIcon as InformationCircleIconSolid  } from "@heroicons/react/24/solid"

export default function Example() {
  return (
    <div className="flex w-full flex-col gap-2">
      <Alert
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        <Typography className="font-medium">Ensure that these requirements are met:</Typography>
        <ul class="mt-2 ml-2 list-disc list-inside">
          <li>At least 10 characters (and up to 100 characters)</li>
          <li>At least one lowercase character</li>
          <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
        </ul>
      </Alert>
      <Alert
        variant="gradient"
        color="red"
        icon={
          <InformationCircleIcon
            strokeWidth={2}
            className="h-6 w-6"
          />
        }
      >
        <Typography className="font-medium">Ensure that these requirements are met:</Typography>
        <ul class="mt-2 ml-2 list-disc list-inside">
          <li>At least 10 characters (and up to 100 characters)</li>
          <li>At least one lowercase character</li>
          <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
        </ul>
      </Alert>
      <Alert
        variant="outlined"
        color="green"
        icon={
          <InformationCircleIconSolid className="h-6 w-6" />
        }
      >
        <Typography className="font-medium">Ensure that these requirements are met:</Typography>
        <ul class="mt-2 ml-2 list-disc list-inside">
          <li>At least 10 characters (and up to 100 characters)</li>
          <li>At least one lowercase character</li>
          <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
        </ul>
      </Alert>
      <Alert
        variant="ghost"
        color="orange"
        icon={
          <InformationCircleIconSolid className="h-6 w-6" />
        }
      >
        <Typography className="font-medium">Ensure that these requirements are met:</Typography>
        <ul class="mt-2 ml-2 list-disc list-inside">
          <li>At least 10 characters (and up to 100 characters)</li>
          <li>At least one lowercase character</li>
          <li>Inclusion of at least one special character, e.g., ! @ # ?</li>
        </ul>
      </Alert>
    </div>
  );
}
```
</CodePreview>

---

## Alert Dismissible

The <Code>Alert</Code> component is a dismissible component that you can control it using the <Code>onClose</Code> and <Code>open</Code> props.

<CodePreview id="alert-dismissible" link="alert#alert-dismissible" component={
  <State initialState={{ open: true }}>
    <Observe>
      {({ setState, open }) => (
        <>
          {!open && (
            <Button
              className="absolute"
              onClick={() => setState((s) => ({ open: true }))}
            >
              Show Alert
            </Button>
          )}
          <Alert
            open={open}
            onClose={() => setState((s) => ({ open: false }))}
          >
            A dismissible alert for showing message.
          </Alert>
        </>
      )}
    </Observe>
  </State>
}>
```jsx
import { useState, Fragment } from "react";
import { Alert, Button } from "@material-tailwind/react";

export default function Example() {
  const [open, setOpen] = useState(true);
  
  return (
    <Fragment>
      {!open && (
        <Button className="absolute" onClick={() => setOpen(true)}>
          Show Alert
        </Button>
      )}
      <Alert open={open} onClose={() => setOpen(false)}>
        A dismissible alert for showing message.
      </Alert>
    </Fragment>
  );
}
````
</CodePreview>

---

## Custom Alert Animation

You can modify the open/close state animation for <Code>Alert</Code> component using the <Code>animate</Code> prop.

<CodePreview id="custom-alert-animation" component={
  <State initialState={{ open: true }}>
    <Observe>
      {({ setState, open }) => (
        <>
          {!open && (
            <Button
              className="absolute"
              onClick={() => setState((s) => ({ open: true }))}
            >
              Show Alert
            </Button>
          )}
          <Alert
            open={open}
            animate={{
              mount: { y: 0 },
              unmount: { y: 100 },
            }}
            onClose={() => setState((s) => ({ open: false }))}
          >
            A dismissible alert with custom animation.
          </Alert>
        </>
      )}
    </Observe>
  </State>
}>
```jsx
import { useState, Fragment } from "react";
import { Alert, Button } from "@material-tailwind/react";

export default function Example() {
  const [open, setOpen] = useState(true);

  return (
    <Fragment>
      {!open && (
        <Button className="absolute" onClick={() => setOpen(true)}>
          Show Alert
        </Button>
      )}
      <Alert
        open={open}
        onClose={() => setOpen(false)}
        animate={{
          mount: { y: 0 },
          unmount: { y: 100 },
        }}
      >
        A dismissible alert with custom animation.
      </Alert>
    </Fragment>
  );
}
```
</CodePreview>

---

## Custom Alert Close Icon

You can modify the close icon for <Code>Alert</Code> component using the <Code>dismissible</Code> prop.

<CodePreview id="custom-alert-close-icon" link="alert#custom-alert-close-icon" component={
  <State initialState={{ open: true }}>
    <Observe>
      {({ setState, open }) => (
        <>
          {!open && (
            <Button
              color="red"
              variant="gradient"
              className="absolute"
              onClick={() => setState((s) => ({ open: true }))}
            >
              Show Alert
            </Button>
          )}
          <Alert
            variant="gradient"
            color="red"
            open={open}
            icon={<SolidIcons.ExclamationTriangleIcon className="h-6 w-6" />}
            open={open}
            action={
              <Button
                variant="text"
                color="white"
                size="sm"
                className="!absolute top-3 right-3"
                onClick={() => setState((s) => ({ open: false }))}
              >
                Close
              </Button>
            }
          >
            Sorry, something went wrong please try again.
          </Alert>
        </>
      )}
    </Observe>
  </State>
}>
```jsx
import React from "react";
import { Alert, Button } from "@material-tailwind/react";
import { ExclamationTriangleIcon } from "@heroicons/react/24/solid";

export default function Example() {
  const [open, setOpen] = React.useState(true);

  return (
    <React.Fragment>
      {!open && (
        <Button className="absolute" color="red" onClick={() => setOpen(true)}>
          Show Alert
        </Button>
      )}
      <Alert
        variant="gradient"
        color="red"
        open={open}
        icon={<SolidIcons.ExclamationTriangleIcon className="h-6 w-6" />}
        open={open}
        action={
          <Button
            variant="text"
            color="white"
            size="sm"
            className="!absolute top-3 right-3"
            onClick={() => setOpen(false)}
          >
            Close
          </Button>
        }
      >
        Sorry, something went wrong please try again.
      </Alert>
    </React.Fragment>
  );
}
```
</CodePreview>

---

## Alert with Content

Use the example below to use <Code>Alert</Code> component with more content like title and description.

<CodePreview id="alert-with-content" link="alert#alert-with-content" className="mt-4" component={
  <State initialState={{ open: true }}>
    <Observe>
      {({ setState, open }) => (
        <>
          {!open && (
            <Button
              color="green"
              className="absolute"
              onClick={() => setState((s) => ({ open: true }))}
            >
              Show Alert
            </Button>
          )}
          <Alert
            open={open}
            color="green"
            className="max-w-screen-md"
            icon={<SolidIcons.CheckCircleIcon className="mt-px h-6 w-6" />}
            onClose={() => setState((s) => ({ open: false }))}
          >
            <Typography variant="h5" color="white">
              Success
            </Typography>
            <Typography color="white" className="mt-2 font-normal">
              I don&apos;t know what that word means. I&apos;m happy. But success,
              that goes back to what in somebody&apos;s eyes success means. For me,
              success is inner peace. That&apos;s a good day for me.
            </Typography>
          </Alert>
        </>
      )}
    </Observe>
  </State>
}>
```jsx
import React from "react";
import { Alert, Button, Typography } from "@material-tailwind/react";
import { CheckCircleIcon } from "@heroicons/react/24/solid";

export default function Example() {
  const [open, setOpen] = React.useState(true);

  return (
    <React.Fragment>
      {!open && (
        <Button
          color="green"
          className="absolute"
          onClick={() => setOpen(true)}
        >
          Show Alert
        </Button>
      )}
      <Alert
        open={open}
        color="green"
        className="max-w-screen-md"
        icon={<CheckCircleIcon className="mt-px h-6 w-6" />}
        onClose={() => setOpen(false)}
      >
        <Typography variant="h5" color="white">
          Success
        </Typography>
        <Typography color="white" className="mt-2 font-normal">
          I don&apos;t know what that word means. I&apos;m happy. But success,
          that goes back to what in somebody&apos;s eyes success means. For me,
          success is inner peace. That&apos;s a good day for me.
        </Typography>
      </Alert>
    </React.Fragment>
  );
}
```
</CodePreview>

<span id="props" className="scroll-mt-48" />
<span id="theme" className="scroll-mt-48" />
