---
title: Tailwind CSS Input for React - Material Tailwind
description: Customise your web projects with our complex inputs component for Tailwind CSS and React using Material Design guidelines.
navigation:
  [
    "input",
    "input-variants",
    "input-sizes",
    "input-colors",
    "input-validations",
    "input-with-icon",
    "disabled-input",
    "props",
    "theme",
  ]
related:
  [
    {
      title: "Props",
      route: "props/input",
      description: "Learn more about props definition and types of input component.",
    },
    {
      title: "Theme",
      route: "theme/input",
      description: "Learn how to customize the theme and styles for input component.",
    },
  ]
github: input
prev: icon-button
next: menu
---

# Tailwind CSS Input - React

Easily create <Code>Input</Code> with different statuses and sizes using our components based on Tailwind CSS and React.

<Code>Input</Code> fields are an essential user interface design element, providing
users with the means to enter non-standardized responses.

See below our <Code>Input</Code> component examples. It comes in different styles and colors, so you can adapt it easily to your needs.

<br />

<CodePreview id="input" link="input#input" component={
  <div className="w-72">
    <Input label="Required" />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="w-72">
      <Input label="Username" />
    </div>
  );
}
```
</CodePreview>

---

## Input Variants

The <Code>Input</Code> component comes with 3 different variants that you can change it using the <Code>variant</Code> prop.

<CodePreview id="input-variants" link="input#input-variants" component={
  <div className="flex flex-col w-72 gap-6">
    <Input variant="static" label="Static" placeholder="Static" />
    <Input variant="standard" label="Standard" />
    <Input variant="outlined" label="Outlined" />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-72 gap-6">
      <Input variant="static" label="Static" placeholder="Static" />
      <Input variant="standard" label="Standard" />
      <Input variant="outlined" label="Outlined" />
    </div>
  );
}
```
</CodePreview>

---

## Input Sizes

The <Code>Input</Code> component comes with 2 different sizes that you can change it using the <Code>size</Code> prop.

<CodePreview id="input-sizes" link="input#input-sizes" component={
  <div className="flex flex-col w-72 items-end gap-6">
    <Input size="md" label="Input Medium" />
    <Input size="lg" label="Input Large" />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-72 items-end gap-6">
      <Input size="md" label="Input Medium" />
      <Input size="lg" label="Input Large" />
    </div>
  );
}
```
</CodePreview>

---

## Input Colors

The <Code>Input</Code> component comes with 19 different colors that you can change it using the <Code>color</Code> prop.

<CodePreview id="input-colors" link="input#input-colors" component={
  <div className="flex w-72 flex-col gap-6">
    <Input color="blue" label="Input Blue" />
    <Input color="purple" label="Input Purple" />
    <Input color="indigo" label="Input Indigo" />
    <Input color="teal" label="Input Teal" />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex w-72 flex-col gap-6">
      <Input color="blue" label="Input Blue" />
      <Input color="purple" label="Input Purple" />
      <Input color="indigo" label="Input Indigo" />
      <Input color="teal" label="Input Teal" />
    </div>
  );
}
```
</CodePreview>

---

## Input Validations

The <Code>Input</Code> component comes with error and success states for performing validations.

<CodePreview id="input-validations" link="input#input-validations" component={
  <div div className="flex flex-col w-72 items-end gap-6">
    <Input label="Input Error" error />
    <Input label="Input Success" success />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-72 items-end gap-6">
      <Input label="Input Error" error />
      <Input label="Input Success" success />
    </div>
  );
}
```
</CodePreview>

---

## Input With icon

You can add icon for the <Code>Input</Code> component using the <Code>icon</Code> prop.

<CodePreview id="input-with-icon" link="input#input-with-icon" component={
  <div className="w-72">
    <Input label="Input With Icon" icon={<i className="fas fa-heart" />} />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="w-72">
      <Input label="Input With Icon" icon={<i className="fas fa-heart" />} />
    </div>
  );
}
```
</CodePreview>

---

## Disabled Input

An <Code>Input</Code> could be disabled as well, it will help you to prevent user interactions like click or focus over the <Code>Input</Code> component.

<CodePreview id="disabled-input" link="input#disabled-input" component={
  <div className="w-72">
    <Input label="Disabled" disabled />
  </div>
}>
```jsx
import { Input } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="w-72">
      <Input label="Disabled" disabled />
    </div>
  );
}
```
</CodePreview>

<span id="props" className="scroll-mt-48" />
<span id="theme" className="scroll-mt-48" />
