---
title: Tailwind CSS Textarea for React - Material Tailwind
description: Customise your web projects with our easy-to-use textarea component for Tailwind CSS and React using Material Design guidelines.
navigation:
  [
    "textarea",
    "textarea-variants",
    "textarea-sizes",
    "textarea-colors",
    "textarea-validations",
    "disabled-textarea",
    "comment-box-textarea",
    "twitter-chatbox-textarea",
    "props",
    "theme",
  ]
related:
  [
    {
      title: "Props",
      route: "props/textarea",
      description: "Learn more about props definition and types of textarea component.",
    },
    {
      title: "Theme",
      route: "theme/textarea",
      description: "Learn how to customize the theme and styles for textarea component.",
    },
  ]
github: textarea
prev: tabs
next: timeline
---

# Tailwind CSS Textarea - React

Easily create <Code>Textarea</Code> with different styles using our component based on Tailwind CSS. It is useful when you want to allow users to enter a sizeable amount of free-form text, for example a comment on a review or feedback form.

See below our example that will help you create a simple <Code>Textarea</Code> for your project. It also comes in different colors and styles, so you can adapt it easily to your needs.

<br />

<CodePreview id="textarea" link="textarea#textarea" component={
  <div className="w-96">
    <Textarea label="Message" />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="w-96">
      <Textarea label="Message" />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="textarea-variants">
## Textarea Variants
</DocsTitle>

The <Code>Textarea</Code> component comes with 3 different variants that you can change it using the <Code>variant</Code> prop.

<CodePreview id="textarea-variants" link="textarea#textarea-variants" component={
  <div className="flex flex-col w-96 gap-6">
    <Textarea variant="static" label="Static" placeholder="Static" />
    <Textarea variant="standard" label="Standard" />
    <Textarea variant="outlined" label="Outlined" />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-96 gap-6">
      <Textarea variant="static" label="Static" placeholder="Static" />
      <Textarea variant="standard" label="Standard" />
      <Textarea variant="outlined" label="Outlined" />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="textarea-sizes">
## Textarea Sizes
</DocsTitle>

The <Code>Textarea</Code> component comes with 2 different sizes that you can change it using the <Code>size</Code> prop.

<CodePreview id="textarea-sizes" link="textarea#textarea-sizes" component={
  <div className="flex flex-col w-96 gap-6">
    <Textarea size="md" label="Textarea Medium" />
    <Textarea size="lg" label="Textarea Large" />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-96 gap-6">
      <Textarea size="md" label="Textarea Medium" />
      <Textarea size="lg" label="Textarea Large" />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="textarea-colors">
## Textarea Colors
</DocsTitle>

The <Code>Textarea</Code> component comes with 19 different colors that you can change it using the <Code>color</Code> prop.

<CodePreview id="textarea-colors" link="textarea#textarea-colors" component={
  <div className="flex w-96 flex-col gap-6">
    <Textarea color="blue" label="Textarea Blue" />
    <Textarea color="purple" label="Textarea Purple" />
    <Textarea color="indigo" label="Textarea Indigo" />
    <Textarea color="teal" label="Textarea Teal" />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex w-96 flex-col gap-6">
      <Textarea color="blue" label="Textarea Blue" />
      <Textarea color="purple" label="Textarea Purple" />
      <Textarea color="indigo" label="Textarea Indigo" />
      <Textarea color="teal" label="Textarea Teal" />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="textarea-validations">
## Textarea Validations
</DocsTitle>

The <Code>Textarea</Code> component comes with error and success states for performing validations.

<CodePreview id="textarea-validations" link="textarea#textarea-validations" component={
  <div className="flex flex-col w-96 gap-6">
    <Textarea label="Textarea Error" error />
    <Textarea label="Textarea Success" success />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="flex flex-col w-96 gap-6">
      <Textarea label="Textarea Error" error />
      <Textarea label="Textarea Success" success />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="disabled-textarea">
## Disabled Textarea
</DocsTitle>

A <Code>Textarea</Code> could be disabled as well, it will help you to prevent user interactions like click or focus over the <Code>Textarea</Code> component.

<CodePreview id="disabled-textarea" link="textarea#disabled-textarea" component={
  <div className="w-96">
    <Textarea label="Disabled" disabled />
  </div>
}>
```jsx
import { Textarea } from "@material-tailwind/react";

export default function Example() {
  return (
    <div className="w-96">
      <Textarea label="Disabled" disabled />
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="comment-box-textarea">
## Comment Box Textarea
</DocsTitle>

Use the example below for a comment box that uses <Code>Textarea</Code> component.

<CodePreview id="comment-box-textarea" component={
  <div className="relative w-[32rem]">
    <Textarea variant="static" placeholder="Your Comment" rows={8} />
    <div className="w-full flex justify-between py-1.5">
      <IconButton variant="text" color="blue-gray" size="sm">
        <OutlineIcons.LinkIcon strokeWidth={2} className="w-4 h-4" />
      </IconButton>
      <div className="flex gap-2">
        <Button
          size="sm"
          color="red"
          variant="text"
          className="rounded-md"
        >
          Cancel
        </Button>
        <Button size="sm" className="rounded-md">Post Comment</Button>
      </div>
    </div>
  </div>
}>
```jsx
import { Textarea, Button, IconButton } from "@material-tailwind/react";
import { LinkIcon } from "@heroicons/react/24/outline";

export default function Example() {
  return (
    <div className="relative w-[32rem]">
      <Textarea variant="static" placeholder="Your Comment" rows={8} />
      <div className="w-full flex justify-between py-1.5">
        <IconButton variant="text" color="blue-gray" size="sm">
          <LinkIcon strokeWidth={2} className="w-4 h-4" />
        </IconButton>
        <div className="flex gap-2">
          <Button
            size="sm"
            color="red"
            variant="text"
            className="rounded-md"
          >
            Cancel
          </Button>
          <Button size="sm" className="rounded-md">Post Comment</Button>
        </div>
      </div>
    </div>
  );
}
```
</CodePreview>

---

<DocsTitle href="twitter-chatbox-textarea">
## Twitter Chatbox Textarea
</DocsTitle>

Use the example below for a chat input that uses <Code>Textarea</Code> component.

<CodePreview id="twitter-chatbox-textarea" component={
  <div className="bg-blue-500/10 border border-blue-gray-500/20 rounded-[99px] w-full flex flex-row items-center gap-2 p-2">
    <div className="flex">
      <IconButton variant="text" className="rounded-full">
        <OutlineIcons.PhotoIcon strokeWidth={2} className="w-5 h-5" />
      </IconButton>
      <IconButton variant="text" className="rounded-full">
        <OutlineIcons.FaceSmileIcon strokeWidth={2} className="w-5 h-5" />
      </IconButton>
    </div>
    <Textarea
      rows={1}
      resize={true}
      placeholder="Your Message"
      className="min-h-full focus:border-transparent !border-0"
      containerProps={{
        className: "grid h-full"
      }}
      labelProps={{
        className: "before:content-none after:content-none"
      }}
    />
    <div>
      <IconButton variant="text" className="rounded-full">
        <OutlineIcons.PaperAirplaneIcon strokeWidth={2} className="w-5 h-5" />
      </IconButton>
    </div>
  </div>
}>
```jsx
import { Textarea, Button, IconButton } from "@material-tailwind/react";
import { LinkIcon } from "@heroicons/react/24/outline";

export default function Example() {
  return (
    <div className="relative w-[32rem]">
      <Textarea variant="static" placeholder="Your Comment" rows={8} />
      <div className="w-full flex justify-between py-1.5">
        <IconButton variant="text" color="blue-gray" size="sm">
          <LinkIcon strokeWidth={2} className="w-4 h-4" />
        </IconButton>
        <div className="flex gap-2">
          <Button
            size="sm"
            color="red"
            variant="text"
            className="rounded-md"
          >
            Cancel
          </Button>
          <Button size="sm" className="rounded-md">Post Comment</Button>
        </div>
      </div>
    </div>
  );
}
```
</CodePreview>

<span id="props" className="scroll-mt-48" />
<span id="theme" className="scroll-mt-48" />
