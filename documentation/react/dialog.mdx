---
title: Dialog - Material Tailwind
description: ReactJS or NextJS Dialog component with Tailwind CSS.
navigation:
  [
    "dialog",
    "dialog-props",
    "dialog-header-props",
    "dialog-body-props",
    "dialog-footer-props",
    "types"
  ]
github: dialog
prev: chip
next: icon-button
---

# Tailwind CSS Dialog - React

Use our Tailwind CSS dialog component to inform users about a task or important information that require decisions, or involves multiple tasks.

A dialog is a type of modal window with critical information which disable all app functionality when they appear and remains on screen until confirmed/dismissed.

See below our dialog example that you can use for your Tailwind CSS and React project.

<br />
<br />
<br />

<State initialState={{ open: false }}>
  <ComponentCard id="dialog">
    <Observe>
      {({ setState, open }) => (
        <>
          <Button
            onClick={() => setState((s) => ({ open: true }))}
            variant="gradient"
          >
            Open Dialog
          </Button>
          <Dialog open={open}>
            <DialogHeader>Its a simple dialog.</DialogHeader>
            <DialogBody divider>
              Lorem, ipsum dolor sit amet consectetur adipisicing elit.
              Accusamus ad reprehenderit omnis perspiciatis aut odit! Unde
              architecto perspiciatis, dolorum dolorem iure quia saepe autem
              accusamus eum praesentium magni corrupti explicabo!
            </DialogBody>
            <DialogFooter>
              <Button
                variant="text"
                color="red"
                onClick={() => setState((s) => ({ open: false }))}
                className="mr-1"
              >
                Cancel
              </Button>
              <Button
                variant="gradient"
                color="green"
                onClick={() => setState((s) => ({ open: false }))}
              >
                Confirm
              </Button>
            </DialogFooter>
          </Dialog>
        </>
      )}
    </Observe>
  </ComponentCard>
</State>

```jsx showLineNumbers
import { useState } from "react";
import {
  Dialog,
  DialogHeader,
  DialogBody,
  DialogFooter
} from "@material-tailwind/react";

export default function Example() {
  const [open, setOpen] = useState(false);

  const handleOpen = (value) => setOpen(!open);

  return (
    <Fragment>
      <Button onClick={handleOpen} variant="gradient">
        Open Dialog
      </Button>
      <Dialog open={open} handler={handleOpen}>
        <DialogHeader>Its a simple dialog.</DialogHeader>
        <DialogBody divider>
          Lorem, ipsum dolor sit amet consectetur adipisicing elit. Accusamus ad
          reprehenderit omnis perspiciatis aut odit! Unde architecto
          perspiciatis, dolorum dolorem iure quia saepe autem accusamus eum
          praesentium magni corrupti explicabo!
        </DialogBody>
        <DialogFooter>
          <Button
            variant="text"
            color="red"
            onClick={handleOpen}
            className="mr-1"
          >
            <span>Cancel</span>
          </Button>
          <Button variant="gradient" color="green" onClick={handleOpen}>
            <span>Confirm</span>
          </Button>
        </DialogFooter>
      </Dialog>
    </Fragment>
  );
}
```

---

## Dialog Props

<span id="dialog-props" className="scroll-mt-48" />
The following props are available for the dialog component. These are the custom
props that come with we've added for the dialog component and you can use all the
other native props as well.

| Attribute   | Type                        | Description                               | Default                               |
| ----------- | --------------------------- | ----------------------------------------- | ------------------------------------- |
| `open`      | <Code small>boolean</Code>  | Open the dialog                           | No default value its a required prop. |
| `handler`   | <Code small>function</Code> | Controls open and close states for dialog | No default value its a required prop. |
| `size`      | [Size](#types)              | Change dialog size                        | <Code small>md</Code>                 |
| `dismiss`   | [Dismiss](#types)           | Change backdrop dismiss listeners         | <Code small>undefined</Code>          |
| `animate`   | [Animate](#types)           | Change dialog animation                   | <Code small>undefined</Code>          |
| `className` | <Code small>string</Code>   | Add custom className for dialog           | `''`                                  |
| `children`  | <Code small>node</Code>     | Add content for dialog                    | No default value its a required prop. |

---

## Dialog Header Props

<span id="dialog-header-props" className="scroll-mt-48" />
The following props are available for the dialog header component. These are the
custom props that come with we've added for the dialog header component and you can
use all the other native props as well.

| Attribute   | Type                      | Description                            | Default                               |
| ----------- | ------------------------- | -------------------------------------- | ------------------------------------- |
| `className` | <Code small>string</Code> | Add custom className for dialog header | `''`                                  |
| `children`  | <Code small>node</Code>   | Add content for dialog header          | No default value its a required prop. |

---

## Dialog Body Props

<span id="dialog-body-props" className="scroll-mt-48" />
The following props are available for the dialog body component. These are the custom
props that come with we've added for the dialog body component and you can use all
the other native props as well.

| Attribute   | Type                       | Description                             | Default                               |
| ----------- | -------------------------- | --------------------------------------- | ------------------------------------- |
| `divider`   | <Code small>boolean</Code> | Add border top & bottom for dialog body | <Code small>false</Code>              |
| `className` | <Code small>string</Code>  | Add custom className for dialog body    | `''`                                  |
| `children`  | <Code small>node</Code>    | Add content for dialog body             | No default value its a required prop. |

---

## Dialog Footer Props

<span id="dialog-footer-props" className="scroll-mt-48" />
The following props are available for the dialog footer component. These are the
custom props that come with we've added for the dialog footer component and you can
use all the other native props as well.

| Attribute   | Type                      | Description                            | Default                               |
| ----------- | ------------------------- | -------------------------------------- | ------------------------------------- |
| `className` | <Code small>string</Code> | Add custom className for dialog footer | `''`                                  |
| `children`  | <Code small>node</Code>   | Add content for dialog footer          | No default value its a required prop. |

---

## Types

<span id="types" className="scroll-mt-48" />

```ts
type size = "xs" | "sm" | "md" | "lg" | "xl" | "xxl";
```

```ts
type dismissType = {
  enabled?: boolean;
  escapeKey?: boolean;
  referencePointerDown?: boolean;
  outsidePointerDown?: boolean;
  ancestorScroll?: boolean;
  bubbles?: boolean;
};
```

```ts
type animate = {
  mount?: Object;
  unmount?: Object;
};
```
