---
title: Material Tailwind Boilerplate - Magic Link Auth
description: Learn how to use Material Tailwind Boilerplate Magic Link Auth Component.
navigation: []
github: magic-link-auth
next: profile-dropdown
---

# Material Tailwind Boilerplate - Magic Link Auth

The magic link auth component is used to provide the user with the option to sign in with their email using a magic link. The magic link auth component is located in the <Code>components/magic-link-auth.tsx</Code> file.

Below is the code snippet of the magic link auth component.

<br />

<CodePreview noPadding component={<Image src="/boilerplate/magic-link-auth.png" alt="magic-link-auth" width={1024} height={1024} className="w-full" />}>
```tsx
import { MagicLinkAuth } from "@/components";

export default function Page() {
  return <MagicLinkAuth />
}
```
</CodePreview>
