type Entry = {
    [key: string]: any;
};
/**
 * Parse object entries from string to array.
 * @param entry Object
 * @returns Object
 */
declare function parseObject(entry: Entry): {};

export { parseObject };
